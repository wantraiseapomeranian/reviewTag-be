<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberQuiz">

	<!-- 내가 풀이한 퀴즈 목록 조회 -->
	<select id="listByAnswerQuiz" resultType="MemberQuizListVO">
		select *
		 from member_quiz
		where quiz_log_member_id = #{loginId}
		order by quiz_log_quiz_id desc
	</select>
	
	<!-- 내가 풀이한 퀴즈 정답률 조회 -->
	<select id="listByAnswerQuizRate" resultType="MemberQuizRateVO">
		select *
		 from member_quiz_rate
		where quiz_log_member_id = #{loginId}
		order by contents_title desc
	</select>


</mapper>